[future-incompat-report]
frequency = "always"

[profile.release]
lto = "fat"
codegen-units = 1

[profile.fast-test]
inherits = "release"
lto = "thin"
debug = true
debug-assertions = true
overflow-checks = true
